---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  interval: 15m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: "59.0.0"
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
  # Aumentamos el timeout para darle tiempo a la BD de migrar
  timeout: 15m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    alertmanager:
      enabled: false

    grafana:
      enabled: true
      admin:
        existingSecret: "grafana-admin-secret"
        userKey: "admin-user"
        passwordKey: "admin-password"

      ingress:
        enabled: true
        ingressClassName: traefik
        hosts:
          - grafana.homelab.local
      
      persistence:
        enabled: true
        storageClassName: "local-path"
      

      # Primero, definimos las variables no sensibles
      env:
        GF_DATABASE_TYPE: "postgres"
        GF_DATABASE_HOST: "postgres-service.default.svc.cluster.local:5432"
        GF_DATABASE_NAME: "grafana"
        GF_DATABASE_SSL_MODE: "disable"

      # Luego, usamos 'envFrom' para cargar TODAS las llaves del secreto
      # como variables de entorno. Este es el método que el chart espera.
      envFrom:
        - secretRef:
            name: grafana-db-credentials

    prometheus:
      enabled: true
      prometheusSpec:
        storageSpec:
          volumeClaimTemplate:
            spec:
              storageClassName: "local-path"
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: "10Gi"