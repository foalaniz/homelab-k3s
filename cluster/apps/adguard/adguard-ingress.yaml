apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: adguard-home
  namespace: networking
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`adguard.homelab.local`)
      kind: Rule
      services:
        - name: adguard-home 
          port: 80