apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: karakeep
  namespace: karakeep
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`karakeep.homelab.local`)
      kind: Rule
      services:
        - name: karakeep-web
          port: 80